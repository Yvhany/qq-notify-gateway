services:
  gateway:
    build: .
    container_name: qq-notify-gateway
    env_file: .env
    environment:
      DATA_DIR: /data
      TZ: Asia/Shanghai
    volumes:
      - ./data:/data
    ports:
      - "18080:8080"
    restart: unless-stopped
