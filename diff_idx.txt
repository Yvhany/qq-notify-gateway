diff --git a/webui/index.html b/webui/index.html
index da5c9ed..ed4115a 100644
--- a/webui/index.html
+++ b/webui/index.html
@@ -204,8 +204,44 @@ pre.code{font-family:ui-monospace,monospace;font-size:12px;background:#fafafa;bo
 
       <!-- 系统配置 -->
       <section class="view" id="v-settings">
-        <div class="page-head"><div><h1>系统配置</h1><p>只读 · 访问控制由反代登录与防火墙承担</p></div><button class="btn primary" onclick="loadConfig()">刷新</button></div>
+        <div class="page-head"><div><h1>系统配置</h1><p>只读 · 访问控制由反代登录与防火墙承担</p></div><button class="btn primary" onclick="loadConfig();loadOpenID()">刷新</button></div>
         <div class="card"><div class="card-b" id="cfgRows">加载中…</div></div>
+
+        <div class="card" style="margin-top:16px">
+          <div class="card-h"><span class="t">三月七小助手接入指引</span><span class="sub">消息推送 → Webhook 渠道四字段</span></div>
+          <div class="card-b">
+            <div class="form-row"><div class="lbl">接收地址</div><div class="ctl"><span class="mono" id="guideUrl" style="flex:1;word-break:break-all">–</span><button class="btn sm" onclick="copyText(document.getElementById('guideUrl').textContent,'地址')">复制</button></div></div>
+            <div class="form-row"><div class="lbl">请求方法</div><div class="ctl"><span class="mono">留空（默认 POST，勿改）</span></div></div>
+            <div class="form-row"><div class="lbl">请求头</div><div class="ctl"><span class="mono">留空（无需认证头）</span></div></div>
+            <div class="form-row" style="border-bottom:none"><div class="lbl">请求体（必填）</div><div class="ctl" style="flex-direction:column;align-items:stretch;gap:8px">
+              <pre class="code" id="guideBody" style="margin:0">{"title":"{title}","content":"{content}","image":"{image}"}</pre>
+              <div><button class="btn sm" onclick="copyText(document.getElementById('guideBody').textContent,'请求体模板')">复制请求体</button>
+              <span style="font-size:12px;color:var(--text-3);margin-left:8px">键名必须是 title/content/image，勿照抄教程示例的 message</span></div>
+            </div></div>
+          </div>
+        </div>
+
+        <div class="card" style="margin-top:16px">
+          <div class="card-h"><span class="t">OpenID 采集与目标切换</span><span class="sub" id="oidStatus">空闲</span></div>
+          <div class="card-b">
+            <div style="display:flex;gap:10px;flex-wrap:wrap;align-items:center">
+              <button class="btn primary" id="oidStartBtn" onclick="oidListen()">开始监听（60 秒）</button>
+              <button class="btn" id="oidStopBtn" onclick="oidStop()" style="display:none">停止监听</button>
+              <span style="font-size:12px;color:var(--text-3)">窗口内给机器人发私聊、拉它进群或在群里 @它，即可抓到加密 OpenID</span>
+            </div>
+            <div class="form-row" style="margin-top:6px"><div class="lbl">单聊 OpenID (c2c)</div><div class="ctl"><span class="mono" id="oidC2C" style="flex:1;word-break:break-all">–</span><button class="btn sm" onclick="fillTarget('c2c',document.getElementById('oidC2C').textContent)">用作目标</button></div></div>
+            <div class="form-row"><div class="lbl">群组 OpenID (group)</div><div class="ctl"><span class="mono" id="oidGroup" style="flex:1;word-break:break-all">–</span><button class="btn sm" onclick="fillTarget('group',document.getElementById('oidGroup').textContent)">用作目标</button></div></div>
+            <div class="form-row" style="border-bottom:none">
+              <div class="lbl">推送目标</div>
+              <div class="ctl">
+                <label style="display:flex;align-items:center;gap:6px;font-size:13px;cursor:pointer"><input type="radio" name="ttype" value="c2c" checked> 单聊</label>
+                <label style="display:flex;align-items:center;gap:6px;font-size:13px;cursor:pointer"><input type="radio" name="ttype" value="group"> 群组</label>
+                <input class="input" id="targetInput" placeholder="OpenID" style="flex:1;min-width:200px">
+                <button class="btn primary" style="white-space:nowrap" onclick="saveTarget()">保存目标</button>
+              </div>
+            </div>
+          </div>
+        </div>
       </section>
 
     </main>
@@ -230,7 +266,7 @@ menus.forEach(function(m){m.addEventListener('click',function(){
   document.getElementById('crumb').textContent=titles[v];
   if(v==='records')loadRecords(1);
   if(v==='logs')loadLogs();
-  if(v==='settings')loadConfig();
+  if(v==='settings'){loadConfig();loadOpenID();}
   if(v==='webhook')loadWebhook();
 });});
 
@@ -335,25 +371,11 @@ async function copyTpl(){
   var text=document.getElementById('tplBlock').textContent;
   try{await navigator.clipboard.writeText(text);toast('已复制 body 模板')}catch(e){toast('复制失败，请手动选择')}
 }
-
-/* ===== 系统日志 ===== */
-var logLines=[],logPinned=true;
-function renderLogs(){
-  var box=document.getElementById('logBox');
-  if(!logLines.length){box.innerHTML='<span class="lt">（暂无日志）</span>';return}
-  box.innerHTML=logLines.map(function(l){
-    var isErr=/失败|FATAL|error|panic/i.test(l);
-    return '<div class="'+(isErr?'err':'')+'">'+esc(l)+'</div>';
-  }).join('');
-  box.scrollTop=box.scrollHeight;
+async function copyText(text,what){
+  try{await navigator.clipboard.writeText(text);toast('已复制'+(what||''))}catch(e){toast('复制失败，请手动选择')}
 }
-async function loadLogs(){
-  try{var d=await jget('/api/logs?n=300');logLines=d.lines||[];renderLogs()}catch(e){toast('加载日志失败')}
-}
-function clearLogView(){logLines=[];renderLogs();toast('视图已清空（磁盘文件不受影响）')}
-function appendLog(line){logLines.push(line);if(logLines.length>1000)logLines.splice(0,logLines.length-1000);renderLogs()}
 
-/* ===== 系统配置（只读） ===== */
+/* ===== 系统配置 ===== */
 async function loadConfig(){
   try{
     var d=await jget('/api/config');var c=d.config;
@@ -374,9 +396,100 @@ async function loadConfig(){
     document.getElementById('cfgRows').innerHTML=rows.map(function(r){
       return '<div class="form-row"><div class="lbl">'+r[0]+'</div><div class="ctl">'+r[1]+'</div></div>';
     }).join('');
+    // 接入指引：优先已保存的公网域名，否则本机地址
+    document.getElementById('guideUrl').textContent=c.webhook_url||((location.origin.replace(/\/$/,''))+'/notify');
+    // 目标切换表单回填
+    document.getElementById('targetInput').value=c.target_openid||'';
+    document.querySelectorAll('input[name=ttype]').forEach(function(r){r.checked=(r.value===c.target_type)});
   }catch(e){toast('加载配置失败')}
 }
 
+/* ===== OpenID 采集 ===== */
+var oidTimer=null;
+async function loadOpenID(){
+  try{
+    var d=await jget('/api/openid');renderOID(d.state);
+  }catch(e){}
+}
+function renderOID(s){
+  var st=document.getElementById('oidStatus');
+  if(s.running){
+    st.textContent='监听中 · 剩余 '+s.remaining_seconds+' 秒';
+    document.getElementById('oidStartBtn').style.display='none';
+    document.getElementById('oidStopBtn').style.display='';
+    if(!oidTimer)oidTimer=setInterval(loadOpenID,1000);
+  }else{
+    st.textContent='空闲';
+    document.getElementById('oidStartBtn').style.display='';
+    document.getElementById('oidStopBtn').style.display='none';
+    if(oidTimer){clearInterval(oidTimer);oidTimer=null}
+  }
+  document.getElementById('oidC2C').textContent=s.c2c||'–';
+  document.getElementById('oidGroup').textContent=s.group||'–';
+}
+async function oidListen(){
+  var d=await jpost('/api/openid/listen');
+  if(!d.ok){toast(d.error||'启动失败');return}
+  toast('监听已启动：60 秒内给机器人发消息/拉群/@它');
+  renderOID(d.state);
+  if(!oidTimer)oidTimer=setInterval(loadOpenID,1000);
+}
+async function oidStop(){
+  await jpost('/api/openid/stop');
+  toast('已停止监听');setTimeout(loadOpenID,200);
+}
+function fillTarget(type,id){
+  if(!id||id==='–'){toast('还没有抓到该类型 OpenID');return}
+  document.querySelectorAll('input[name=ttype]').forEach(function(r){r.checked=(r.value===type)});
+  document.getElementById('targetInput').value=id;
+  toast('已填入，点击「保存目标」生效');
+}
+async function saveTarget(){
+  var type=(document.querySelector('input[name=ttype]:checked')||{}).value||'c2c';
+  var id=document.getElementById('targetInput').value.trim();
+  if(!id){toast('请填写 OpenID');return}
+  var d=await jput('/api/target',{target_type:type,target_openid:id});
+  if(!d.ok){toast('保存失败: '+(d.error||''));return}
+  toast('推送目标已切换为 '+(type==='c2c'?'单聊':'群组'));
+  loadConfig();
+}
+async function jpost(u){var r=await fetch(u,{method:'POST'});return r.json()}
+
+/* ===== 系统日志 ===== */
+var logLines=[],logPinned=true;
+function renderLogs(){
+  var box=document.getElementById('logBox');
+  if(!logLines.length){box.innerHTML='<span class="lt">（暂无日志）</span>';return}
+  box.innerHTML=logLines.map(function(l){
+    var isErr=/失败|FATAL|error|panic/i.test(l);
+    return '<div class="'+(isErr?'err':'')+'">'+esc(l)+'</div>';
+  }).join('');
+  box.scrollTop=box.scrollHeight;
+}
+async function loadLogs(){
+  try{var d=await jget('/api/logs?n=300');logLines=d.lines||[];renderLogs()}catch(e){toast('加载日志失败')}
+}
+function clearLogView(){logLines=[];renderLogs();toast('视图已清空（磁盘文件不受影响）')}
+function appendLog(line){logLines.push(line);if(logLines.length>1000)logLines.splice(0,logLines.length-1000);renderLogs()}
+
+/* ===== WS 新事件：openid / 状态 / 目标 ===== */
+function onOpenIDEvent(ev){
+  if(ev.type==='openid'){
+    toast('抓到 '+(ev.data.kind==='c2c'?'单聊':'群组')+' OpenID');
+    loadOpenID();
+    // 顺手把抓到的填进目标表单（不自动保存）
+    fillTargetSilent(ev.data.kind, ev.data.id);
+  }else if(ev.type==='openid_state'){
+    renderOID(ev.data);
+  }else if(ev.type==='target'){
+    loadConfig();
+  }
+}
+function fillTargetSilent(type,id){
+  document.querySelectorAll('input[name=ttype]').forEach(function(r){r.checked=(r.value===type)});
+  document.getElementById('targetInput').value=id;
+}
+
 /* ===== WebSocket：仅页面可见时连接 ===== */
 var ws=null,retry=0;
 function setWsDot(on,txt){var d=document.getElementById('wsDot');d.className='ws-dot'+(on?' on':'');d.innerHTML='<i></i>'+txt;var s=document.getElementById('stWs');if(s)s.textContent=txt}
@@ -393,6 +506,7 @@ function wsConnect(){
       else if(ev.type==='stats'){renderStats(ev.data)}
       else if(ev.type==='log'){appendLog(ev.data)}
       else if(ev.type==='webhook'){loadWebhook()}
+      else if(ev.type==='openid'||ev.type==='openid_state'||ev.type==='target'){onOpenIDEvent(ev)}
     }catch(err){}
   };
   ws.onclose=function(){ws=null;setWsDot(false,'实时断开');
@@ -405,7 +519,7 @@ window.addEventListener('pagehide',wsDisconnect);
 window.addEventListener('resize',function(){if(lastStats)drawTrend(lastStats.daily_counts||[])});
 
 /* ===== 初始化 ===== */
-loadStats();loadRecords(1);loadWebhook();loadLogs();loadConfig();
+loadStats();loadRecords(1);loadWebhook();loadLogs();loadConfig();loadOpenID();
 wsConnect();
 </script>
 </body>
